<%-include('../includes/head.ejs')%>
<link rel="stylesheet" href="/css/product.css">
<link rel="stylesheet" href="/css/forms.css">
</head>


<%-include('../includes/navigation.ejs')%>

<main>
    <form class="product-form" action="/admin/<% if (editing){%>edit-student<%}else{ %>add-student<% }%>" method="POST">
        <div class="form-control">
            <label for="title">Name</label>
            <input type="text" name="name" id="name" value="<% if (editing) { %> <%=student.name%> <% } %>">
        </div>
        <div class="form-control">
            <label for="price">Address</label>
            <input type="text" name="address" id="address" value="<%if(editing){%> <%=student.address%><%}%>">

        <% if(editing) {%>

        <!--<input type=" hidden" value="<%= student.id %>" name="studentId"> -->
            <input type="hidden" value="<%= student.id %>" name="studentId">
            <% } %>
            <button class="btn" type="submit"><% if(editing) { %> Update student<% } else{ %>Add student
                <% } %></button>

    </form>
</main>
<%-include('../includes/end.ejs')%>